<?xml version="1.0" encoding="UTF-8"?>

<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="LOG.HOME" value="${app.logs}"/>
    <property name="LOG.PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{requestId:-none}] %-5level %logger{36} - %msg%n"/>
    <property name="LOG.PATTERN.CALLER" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{requestId:-none}] %-5level [%thread] %replace(%caller{1}){'\t|Caller.{1}0|\r\n', ''} - %msg%n"/>

    <!-- 日志输出的通道 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- requestId 或者其他，通过 MDC 可以打印-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}  [%X{requestId:-none}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--日志输出的通道，file -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>user.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{requestId}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.jtfr" level="DEBUG"/>
    <logger name="org.springframework.web" level="INFO"/>

    <root>
        <level value="INFO"/>
        <!-- 指定输出类型。ref 引用-->
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>